import React from 'react';
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Our Stories',
};

const Blog: React.FC = () => {
    return (
   <>
   </>
  );
};

export default Blog;
